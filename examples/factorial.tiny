// Factorial — recursive function + a while-loop based check.

function factorial(n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

let i = 1;
while (i <= 8) {
    print("factorial(" + i + ") = " + factorial(i));
    i = i + 1;
}
